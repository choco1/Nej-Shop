{% extends 'base.html.twig' %}

{% block title %}Hello AdminController!{% endblock %}

{% block body %}
    <div class="container">
        <h1>Creation d'un produit</h1>

        {{ form_start(formProduct) }}

        {{ form_widget(formProduct) }}

        <button type="submit" class="btn btn-success mr-4">Modifier votre produit</button>
        <a href="{{ path('delete_product', { id: product.id }) }}" class="ml-4 btn btn-danger">
            Supprimer votre produit
        </a>
        {{ form_end(formProduct) }}
    </div>

{% endblock %}